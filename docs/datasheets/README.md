# Datasheet evidence

Active project document registrations are in `../../embedr.yaml`. Some retained
legacy registrations are not searchable through the cloud API. Newly indexed
documents and their exact part mappings are recorded in the Zen source.

Removed-part documents were archived, not destroyed. See
`../archive/pre-bom-finalization/` for prior entries and PDFs. Local PDFs in this
directory support retained components or fixtures; their presence is not a
claim that they are the newest manufacturer revision.